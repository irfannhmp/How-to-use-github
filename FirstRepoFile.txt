This is First Repo text file 

first of all configure github user
git config --global user.name "yourusername"
git config --global user.email "youremail@mail.com"


git add (file name) // to upload single file
git add * // to upload complete folder
git commit -m "your commit message"
git push

